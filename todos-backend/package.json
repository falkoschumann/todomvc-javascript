{
  "name": "todos-backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "type": "module",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest src",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch-all src",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules jest src --coverage"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  },
  "jest": {
    "collectCoverage": false,
    "transform": {}
  }
}
